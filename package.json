{
  "name": "forgo-examples",
  "private": true,
  "dependencies": {
    "forgo": "^0.0.10",
    "forgo-router": "^0.0.3"
  },
  "devDependencies": {
    "@babel/core": "^7.12.10",
    "@babel/preset-typescript": "^7.12.7",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "start": "rm -rf dist/ && cp -r public/ dist/ && parcel public/index.html",
    "start-todos": "rm -rf dist/ && cp -r public/ dist/ && parcel public/todos.html",
    "start-routing": "rm -rf dist/ && cp -r public/ dist/ && parcel public/routing.html"
  }
}
